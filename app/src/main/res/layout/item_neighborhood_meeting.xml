<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/iv_neighborhood_meeting"
        android:layout_width="54dp"
        android:layout_height="54dp"
        android:layout_marginHorizontal="6dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@color/carrot_black" />

    <TextView
        android:id="@+id/tv_neighborhood_meeting"
        style="@style/TextAppearance.AppleSDGothic.Info"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:ellipsize="end"
        android:maxLines="2"
        android:textColor="@color/carrot_black"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="@id/iv_neighborhood_meeting"
        app:layout_constraintStart_toStartOf="@id/iv_neighborhood_meeting"
        app:layout_constraintTop_toBottomOf="@id/iv_neighborhood_meeting"
        tools:text="2030 세대만 등산모임" />

</androidx.constraintlayout.widget.ConstraintLayout>